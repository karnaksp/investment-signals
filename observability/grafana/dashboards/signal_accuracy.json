{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prom"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "options": {"legend": {"displayMode": "list", "placement": "bottom"}},
      "targets": [
        {
          "expr": "sum(rate(detector_messages_processed_total[5m])) by (outcome)",
          "legendFormat": "{{outcome}}",
          "refId": "A"
        }
      ],
      "title": "Detector throughput (msg/s) by outcome",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prom"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "options": {"legend": {"displayMode": "list", "placement": "bottom"}},
      "targets": [
        {
          "expr": "sum(rate(detector_signals_emitted_total[5m])) by (signal_type)",
          "legendFormat": "{{signal_type}}",
          "refId": "A"
        }
      ],
      "title": "Signals emitted / sec (proxy for alpha volume)",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prom"},
      "fieldConfig": {"defaults": {}, "overrides": []},
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8},
      "id": 3,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(detector_process_event_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95 process latency",
          "refId": "A"
        }
      ],
      "title": "Detector hot-path latency p95 (s)",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prom"},
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "min": 0,
          "max": 1
        },
        "overrides": []
      },
      "gridPos": {"h": 7, "w": 12, "x": 0, "y": 16},
      "id": 4,
      "targets": [
        {
          "expr": "signal_labeller_directional_hit_rate",
          "legendFormat": "single-horizon JSON",
          "refId": "A"
        }
      ],
      "title": "Offline labeller: directional hit rate (duckdb_label_signals → JSON file)",
      "type": "gauge"
    },
    {
      "datasource": {"type": "prometheus", "uid": "prom"},
      "fieldConfig": {
        "defaults": {"unit": "percentunit", "min": 0, "max": 1},
        "overrides": []
      },
      "gridPos": {"h": 7, "w": 12, "x": 12, "y": 16},
      "id": 5,
      "options": {"legend": {"displayMode": "list", "placement": "bottom"}},
      "targets": [
        {
          "expr": "signal_labeller_directional_hit_rate_by_horizon",
          "legendFormat": "h={{horizon}}",
          "refId": "A"
        }
      ],
      "title": "Hit rate by horizon (multi --forward-bars)",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["signal-engine"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "timepicker": {},
  "timezone": "browser",
  "title": "Signal pipeline & quality",
  "uid": "signal-accuracy-proxy",
  "version": 2,
  "weekStart": ""
}
